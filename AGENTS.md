# OFFICESTRA 저장소 작업 지침

- `checklist.md`와 `context-notes.md`는 v1.0 이전 작업 기록의 동결본이다. 읽을 수 있지만 새 내용을 추가하거나 수정·재생성하지 않는다.
- 비자명한 업무의 계획과 결과는 대화에 간결하게 보고한다. 완료 턴은 백엔드가 PostgreSQL `work_records`에 자동 저장한다.
- 현재 작업 기록은 읽기 전용 `GET /api/work-records`로 조회하고, 필요하면 `state`, `kind`, `q` 필터를 사용한다.

## 코드 수정 방식

- 코드는 원본 저장소에서 직접 수정한다. 검증용 worktree를 따로 만들지 않는다.
- 파일 수정에는 Edit·Write 도구를 쓴다. Bash에서 `sed`나 스크립트로 치환하면 대화 화면에 편집 카드가 남지 않아 사용자가 무엇이 바뀌었는지 볼 수 없다.
- 수정 뒤 관련 테스트를 돌리고 결과를 있는 그대로 보고한다. 건너뛴 테스트를 통과로 세지 않는다.
- 앱 반영이 필요하면 재빌드까지 하고 사용자에게 알린다. 백엔드(포트 4317) 재시작은 사용자가 앱 화면 버튼으로 직접 하므로 `launchctl`로 대신 처리하지 않는다.
- 커밋·병합·푸시는 사용자가 지시할 때만 한다.
- 여러 직원이 같은 폴더를 함께 쓰므로 고친 파일 목록을 보고에 반드시 남긴다.

## 검증과 릴리즈

- 자동 테스트는 로컬에서 수행한다. GitHub Actions의 CI 자동 검증은 사용하지 않는다.
- 사용자가 앱을 직접 사용해 확인한 뒤 릴리즈를 지시한다. 로컬 테스트 통과를 사용자 확인으로 대신하지 않는다.
- 릴리즈 준비 중 별도 수정이 필요하면 먼저 보고한다. 릴리즈 요청만으로 앱 기능 수정까지 확대하지 않는다.

## 자주 쓰는 명령

명령 출력은 읽은 파일과 똑같이 대화에 쌓여 남은 턴마다 다시 전송된다. 조용한 형태로 실행한다.

- 백엔드 테스트 전체 — `cd backend && node --test 2>&1 | grep -vE "^✔ " | tail -25`
  통과 줄만 걷어내고 요약·건너뜀·실패는 그대로 남는다. 420줄이 12줄이 된다.
- 백엔드 한 파일만 — `cd backend && node --test test/<파일> 2>&1 | grep -vE "^✔ " | tail -15`
- Swift 테스트 — `swift test` 그대로 쓴다. 기본 출력이 20줄 안쪽이라 걸러낼 것이 없다.

## 환경과 데이터 조회

- zsh에서 파일 패턴을 명령 인자로 넘길 때는 `--glob '*.mjs'`처럼 따옴표로 감싼다.
- 기록·상태 조회는 읽기 전용 API를 우선 사용한다: `GET /api/work-records`, `/api/live-feed`, `/api/active-sessions`, `/api/characters`. 필요한 항목만 출력한다.
- 직접 DB 조회가 필요하면 `backend/src/db.mjs`의 `pool`을 재사용한다. 조회 전용 작업은 같은 연결에서 `BEGIN READ ONLY`로 시작하고, `finally`에서 트랜잭션 정리·연결 반환·독립 조회 프로세스의 풀 종료를 보장한다.
- 테이블·컬럼명은 추측하지 않는다. 필요한 경우 `information_schema.columns`로 실제 스키마를 확인한다.
- pg의 bigint·numeric은 문자열로 반환될 수 있다. 비교·합산 전에 용도에 맞게 변환하되, 큰 정수·정밀 금액을 무조건 `Number()`로 변환하지 않는다.
- Claude 비용은 앱에서 토큰으로 재계산하지 않고 CLI 보고 금액을 사용한다. 보고값이 없으면 비용을 만들어내지 않는다(`backend/src/token-cost-estimator.mjs`).

## 컨텍스트 압축 지침

- 압축 요약에는 OFFICESTRA 직원 이름과 직급 호칭을 쓰지 않는다.
- 사용자는 `사용자`로 표현하고, 직원의 발언과 행동은 이름 없이 내용과 결과 중심으로 기록한다.
